# [SAAD] Accepted, not suppressed. Both need a starlette version FastAPI
# 0.121.1 does not accept: it requires >=0.40.0,<0.50.0, and the fixes land in
# 1.1.0 and 1.3.1. Neither is reachable in this application. CVE-2026-48818 is
# SSRF via UNC paths in StaticFiles, and this app mounts no static files.
# CVE-2026-54283 is a form parsing limit bypass, and no endpoint accepts form
# data. Revisit when FastAPI widens its constraint.
CVE-2026-48818
CVE-2026-54283