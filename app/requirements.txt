fastapi==0.121.1
uvicorn[standard]==0.30.6
prometheus-client==0.20.0
# [SAAD] starlette pinned explicitly rather than left to FastAPI's range.
# Trivy flagged three HIGH findings in 0.38.6. 0.49.0 is the highest version
# FastAPI 0.121.1 accepts, since it requires starlette >=0.40.0,<0.50.0.
# CVE-2024-47874 is cleared. CVE-2026-48818 and CVE-2026-54283 need 1.1.0 and
# 1.3.1, which no current FastAPI release allows. Both are recorded in
# .trivyignore with that constraint named.
starlette==0.49.0